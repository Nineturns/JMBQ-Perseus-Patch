name: JMBQ-Perseus Build

on:
  workflow_dispatch:
    inputs:
      apk_url:
        description: 'APK下载链接（只能填入直链，最好使用国外云盘的直链，例如Google云盘）'
        required: true
        type: string
        default: ''
      mod_url:
        description: 'JMBQ_MOD_MENU下载链接（填入直链，同上）'
        required: true
        type: string
        default: ''

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Setup Java (JDK 17)
        uses: actions/setup-java@v3
        with:
          java-version: "17"
          distribution: "adopt"

      - name: Setup Android SDK (platform-tools, build-tools, ndk)
        uses: android-actions/setup-android@v3
        with:
          api-level: 33
          components: |
            platform-tools
            build-tools;32.0.0
            ndk;21.4.7075529
            cmdline-tools;latest

      - name: Install system packages (7zip, unzip, jq, wget)
        run: |
          sudo apt-get update
          sudo apt-get install -y tar p7zip-full unzip jq wget zip netcat-openbsd

      - name: Make repo bin/ executables and add to PATH
        run: |
          # Ensure repo/bin exists and is executable
          if [ -d "$GITHUB_WORKSPACE/bin" ]; then
            chmod +x "$GITHUB_WORKSPACE/bin"/* || true
            echo "$GITHUB_WORKSPACE/bin" >> $GITHUB_PATH
            ls -lah "$GITHUB_WORKSPACE/bin"
          else
            echo "No bin/ directory found in repo root"
          fi

      - name: Ensure Android tools (ndk/build-tools) are in PATH
        run: |
          # Add first found NDK folder to PATH (if present)
          if [ -d "$ANDROID_SDK_ROOT/ndk" ]; then
            ndk_dir=$(ls -1 "$ANDROID_SDK_ROOT/ndk" | head -n1)
            echo "Found NDK: $ndk_dir"
            echo "$ANDROID_SDK_ROOT/ndk/$ndk_dir" >> $GITHUB_PATH
          fi

          # Add build-tools (prefer 32.0.0)
          if [ -d "$ANDROID_SDK_ROOT/build-tools/32.0.0" ]; then
            echo "$ANDROID_SDK_ROOT/build-tools/32.0.0" >> $GITHUB_PATH
          else
            for d in "$ANDROID_SDK_ROOT"/build-tools/*; do
              [ -d "$d" ] && echo "$d" >> $GITHUB_PATH && break
            done
          fi

          # platform-tools
          if [ -d "$ANDROID_SDK_ROOT/platform-tools" ]; then
            echo "$ANDROID_SDK_ROOT/platform-tools" >> $GITHUB_PATH
          fi

      - name: Show environment & verify tools (debug)
        run: |
          echo "WORKSPACE=$GITHUB_WORKSPACE"
          echo "JAVA_HOME=$JAVA_HOME"
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT"
          which java || true
          java -version || true
          which python3 || true
          python3 -V || true
          which 7zz || true
          which rar || true
          which apkeep || true
          which apktool || true
          which zipalign || true
          which apksigner || true
          ndk-build --version || true
          ls -lah packages || true
          ls -lah bin || true

      - name: Install Python deps (if any) and patch
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install requests
          python3 JMBQ-Perseus-Patch.py \
            --apk-url "${{ github.event.inputs.apk_url }}" \
            --mod-url "${{ github.event.inputs.mod_url }}"
        env:
          ANDROID_SDK_ROOT: ${{ env.ANDROID_SDK_ROOT }}
          JAVA_HOME: ${{ env.JAVA_HOME }}

      - name: Set JMBQ_PERSEUS_VERSION env from built apk filename
        run: |
          libfile=$(ls temp_MOD_MENU* 2>/dev/null | head -n1 || true)
          if [ -z "$libfile" ]; then
            echo "No libfile found - skipping PERSEUS_VERSION set"
            echo "PERSEUS_VERSION=unknown" >> $GITHUB_ENV
          else
            version=$(basename "$libfile" | cut -d'_' -f 4 | sed 's/\.[^.]*$//')
            echo "Built lib file: $libfile"
            echo "PERSEUS_VERSION=$version" >> $GITHUB_ENV
          fi

      - name: Create Draft Release with APK(s)
        uses: marvinpinto/action-automatic-releases@latest
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: "latest"
          draft: false
          title: "V${{ env.PERSEUS_VERSION }} APK Patched"
          files: |
            apk_build/*patched.apk
